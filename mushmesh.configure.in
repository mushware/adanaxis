dnl Process this file with autoconf to produce a configure script.
AC_PREREQ(2.52)
AC_INIT(README)
AC_CANONICAL_HOST
AC_CANONICAL_TARGET

MUSHMESH_MAJOR_VERSION=0
MUSHMESH_MINOR_VERSION=0
MUSHMESH_MICRO_VERSION=1
MUSHMESH_VERSION=$MUSHMESH_MAJOR_VERSION.$MUSHMESH_MINOR_VERSION.$MUSHMESH_MICRO_VERSION
MUSHMESH_UNDERSCORE_VERSION=${MUSHMESH_MAJOR_VERSION}_${MUSHMESH_MINOR_VERSION}_${MUSHMESH_MICRO_VERSION}

AC_SUBST(MUSHMESH_MAJOR_VERSION)
AC_SUBST(MUSHMESH_MINOR_VERSION)
AC_SUBST(MUSHMESH_MICRO_VERSION)
AC_SUBST(MUSHMESH_VERSION)
AC_SUBST(MUSHMESH_UNDERSCORE_VERSION)

AM_INIT_AUTOMAKE(mushmesh,$MUSHMESH_VERSION)
TOP_SUBDIRS="src"
AC_SUBST(TOP_SUBDIRS)
EXTRA_DIST=""
AC_SUBST(EXTRA_DIST)
AM_CONFIG_HEADER(config.h)

dnl Checks for programs.
AC_PROG_MAKE_SET
AC_PROG_CXX
AC_PROG_INSTALL
AC_PROG_LN_S
AC_DISABLE_SHARED
AC_PROG_LIBTOOL

AC_LANG(C++)

dnl Checks for libraries.
AC_CHECK_HEADER(windows.h)

AC_MSG_NOTICE([****************************************************************])
AC_MSG_NOTICE([*                                                              *])
AC_MSG_NOTICE([* Choosing a platform pack.  Please check that this is correct *])
AC_MSG_NOTICE([*                                                              *])
AC_MSG_NOTICE([****************************************************************])

case "$target" in
    powerpc-apple-darwin*)
        AC_MSG_NOTICE([* Assuming Darwin platform is MacOS X.])
 #       LIBS="$LIBS -framework Carbon"
 #       CPPFLAGS="$CPPFLAGS -fpascal-strings"
        ;;
    *)
        if test "x$ac_cv_header_windows_h" = "xyes" ; then
            AC_MSG_NOTICE([* Header windows.h detected.  Assuming target is Windows.])
           CPPFLAGS="$CPPFLAGS"
        else
            AC_MSG_NOTICE([* Assuming generic X Windows platform.])
            CPPFLAGS="$CPPFLAGS"
        fi
        ;;
esac

dnl Check for libraries

dnl Checks for header files
AC_CHECK_HEADERS(Mushcore/Mushcore.h,, AC_MSG_ERROR([Cannot find Mushcore.h library header.  Build requires Mushcore library - see http://www.mushware.com/]))

dnl Checks for header files
AC_CHECK_HEADERS(sstream,,[
AC_MSG_WARN([sstream not found.  Using copy from distribution])
CPPFLAGS="$CPPFLAGS -I\${srcdir}/Support/gcc-2.95.2"
])

AC_CHECK_HEADERS(valarray,,[
AC_MSG_WARN([valarray not found.  Will fall back to vector])
])

AC_CHECK_HEADERS(unistd.h)
AC_CHECK_HEADERS(sys/wait.h)
AC_CHECK_HEADERS(sys/param.h)
AC_CHECK_HEADERS(sys/types.h)

dnl Checks for typedefs, structures, and compiler characteristics.
AC_C_BIGENDIAN

dnl Check that autogenerated Makefile.ams are there
AC_CHECK_FILE(src,[
AC_CHECK_FILE(src/Makefile.am,,[
AC_MSG_ERROR([src/Makefile.am not present.  Please run autogen.sh])
])
])

AC_OUTPUT(Makefile src/Makefile)
AC_MSG_NOTICE([Verdict was:])
AC_MSG_NOTICE([CPPFLAGS=$CPPFLAGS])
AC_MSG_NOTICE([LDFLAGS=$LDFLAGS])
AC_MSG_NOTICE([LIBS=$LIBS])
