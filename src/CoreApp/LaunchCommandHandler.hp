/*
 * $Id$
 * $Log$
 */
 
#include "CommandHandler.hp"
 
class LaunchCommandHandler : public CommandHandler
{
public:
    bool HandleCommand(const string& str);
    static void Install(void);
    static LaunchCommandHandler& Instance(void) {return *((m_instance==NULL)?m_instance=new LaunchCommandHandler:m_instance);}
    
protected:    
    void Launch(const string& inStr);
    LaunchCommandHandler() {} // Prevent construction by client

private:
    static LaunchCommandHandler *m_instance;
};
