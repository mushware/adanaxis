/*
 * $Id$
 * $Log$
 */

#include "CommandHandler.hp"
class GLContext;

class GLCommandHandler : public CommandHandler
{
public:
    bool HandleCommand(const string& str);
    static void Install(void);
    static GLCommandHandler& Instance(void) {return *((m_instance==NULL)?m_instance=new GLCommandHandler:m_instance);}

protected:
    GLCommandHandler() {}

private:
    void InitGL(const string& inStr);
    void LoadPixmap(const string& inStr);
    
    static GLCommandHandler *m_instance;
};
